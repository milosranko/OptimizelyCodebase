@using Optimizely.Demo.Core.Models.Definitions
@model IEnumerable<NameValueStringDefinition>

@if (Model != null && Model.Any())
{
    foreach (var item in Model)
    {
        @Html.Raw($"<!--{item.Name}-->")
        @Html.Raw(item.Value)
    }
}
